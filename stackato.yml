name: fastHAR-api

instances: 1

framework:
    type: node10

mem: 128

hooks:
    pre-staging:
        - sudo apt-get install phantomjs

processes:
    web: npm start

services:
    ${name}-redis: redis
